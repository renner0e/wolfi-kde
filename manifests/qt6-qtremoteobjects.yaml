package:
  name: qt6-qtremoteobjects
  version: "6.10.0"
  epoch: 0
  description: qtremoteobjects
  copyright:
    - license: LGPL-3.0-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - qt6-qtconnectivity-dev
      - qt6-qtdeclarative-dev
      - qt6-qtwebsockets-dev
      - vulkan-headers
      - vulkan-loader

var-transforms:
  - from: ${{package.version}}
    match: "_"
    replace: "-"
    to: mangled-package-version

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/qt/qtremoteobjects
      tag: v${{vars.mangled-package-version}}
      expected-commit: c200d8c5b3de18ea1497066f74f56fdf7cd42396

  - uses: qt6/configure

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  github:
    identifier: qt/qtremoteobjects
    use-tag: true
    strip-prefix: v
  ignore-regex-patterns:
    - 'rc\d+$'
    - 'beta\d+$'

subpackages:
  - name: ${{package.name}}-dev
    description: ${{package.name}} development libraries and headers
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/tw/ldd-check
        - uses: test/pkgconf

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/pkgconf
