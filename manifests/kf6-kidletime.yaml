package:
  name: kf6-kidletime
  version: "6.19.0"
  epoch: 0
  description: KIdleTime
  copyright:
    - license: C0-1.0 AND GPL-2.0-or-later AND LGPL-2.1-or-later AND MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - extra-cmake-modules
      - libglvnd-dev
      - plasma-wayland-protocols-dev
      - qt6-qtbase-dev
      - qt6-qtwayland-dev
      - vulkan-headers
      - wayland-dev
      - wayland-protocols

pipeline:
  - uses: git-checkout
    with:
      expected-commit: d3cdda7f8e30d29346dcbd739cda1cecb551232a
      repository: https://invent.kde.org/frameworks/kidletime
      tag: v${{package.version}}

  - uses: cmake/configure
    with:
      opts: |
        -DECM_DIR=/usr/share/ECM/ \
        -DWITH_X11=OFF

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  git:
    strip-prefix: v

subpackages:
  - name: ${{package.name}}-dev
    description: ${{package.name}} development libraries and headers
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/tw/ldd-check

test:
  pipeline:
    - uses: test/tw/ldd-check

