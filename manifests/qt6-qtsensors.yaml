package:
  name: qt6-qtsensors
  version: "6.10.0"
  epoch: 0
  description: qtsensors
  copyright:
    - license: LGPL-3.0-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - qt6-qtbase-dev
      - libglvnd-dev
      - libxkbcommon-dev
      - qt6-qtsvg-dev
      - qt6-qtdeclarative-dev
      - vulkan-headers

var-transforms:
  - from: ${{package.version}}
    match: "_"
    replace: "-"
    to: mangled-package-version

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/qt/qtsensors
      tag: v${{vars.mangled-package-version}}
      expected-commit: 33205facb4c0ce9dbfdf906db4fb81627561bba5

  - uses: qt6/configure

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  github:
    identifier: qt/qtsensors
    use-tag: true
    strip-prefix: v
  ignore-regex-patterns:
    - 'rc\d+$'
    - 'beta\d+$'

subpackages:
  - name: ${{package.name}}-dev
    description: ${{package.name}} development libraries and headers
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/tw/ldd-check
        - uses: test/pkgconf

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/pkgconf
