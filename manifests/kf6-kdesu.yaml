package:
  name: kf6-kdesu
  version: "6.18.0"
  epoch: 0
  description: KDE Su
  copyright:
    - license: BSD-2-Clause AND BSD-3-Clause AND CC0-1.0 AND GPL-2.0-only AND GPL-2.0-or-later AND GPL-3.0-only AND GPL-3.0-or-later AND LGPL-2.0-only AND LGPL-2.0-or-later AND LGPL-2.1-only AND LGPL-2.1-or-later AND LGPL-3.0-only AND (GPL-2.0-only OR GPL-3.0-only) AND (LGPL-2.1-only OR LGPL-3.0-only) AND MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - extra-cmake-modules
#      - libglvnd-dev
      - qt6-qtbase-dev
      - kf6-kconfig-dev
      - qt6-qtdeclarative-dev
      - kf6-kcoreaddons-dev
      - kf6-ki18n-dev
      - kf6-kpty-dev
#      - vulkan-headers
#      - qt6-qttools-dev

pipeline:
  - uses: git-checkout
    with:
      expected-commit: a2e11795257291e7ee84859779e3cf3f5429f8b5
      repository: https://invent.kde.org/frameworks/kdesu
      tag: v${{package.version}}

  - uses: cmake/configure
    with:
      opts: |
        -DECM_DIR=/usr/share/ECM/ \
        -DHAVE_X11=OFF

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  git:
    strip-prefix: v

subpackages:
  - name: ${{package.name}}-dev
    description: ${{package.name}} development libraries and headers
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/tw/ldd-check

test:
  pipeline:
    - uses: test/tw/ldd-check

